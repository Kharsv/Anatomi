package com.kharsh;

import java.awt.*;
/*
public class CentrOkruzhnosti {
    public static double xu;
    public static double yu;
    public static double x;
    public static double y;
    public static void centrOkruzhnosti() {
        CoordinateSystem cs = new CoordinateSystem();
        LinesComponent comp = new LinesComponent();
        Ocenka oc = new Ocenka();
        Font font = new Font("Times new Roman", Font.PLAIN, 25);
        comp.addCoordinateSysytem();
        comp.coordsys = true;
        comp.logoTip = true;
        Point p = cs.rand2();
        comp.addCircle(p.getX(), p.getY(), 80, 80, 5.f);
        x = p.getX()/cs.mm - cs.orx/cs.mm;
        y = -p.getY()/cs.mm + cs.ory/cs.mm;
        if(cs.SubTopic == 0) {
            String string = String.format("Координаты центра окружности на рисунке х = %.1f, y = %.1f", x, y);
            comp.addPString(string, cs.downTextLocation, font);
            comp.repaint();
        }
    }
}
*/